# Exports / local data
*.csv

# Local backups — never commit these
backups/

# Claude session files
.claude/

# OS junk
.DS_Store
Thumbs.db

# Local serve scripts (not needed on Netlify)
serve*.bat
serve*.ps1
serve*.sh

# Local env for netlify dev
.env
.env.local

# Local Netlify folder
.netlify

# Local debug / one-off scripts — may contain credentials, never commit
_test_*.js
_check_*.js
_check_*.py
_exchange_code.js
_rebuild.py
_pickup.ps1
*.patch

# Secrets — never commit
_session-logs/Authtoken.txt

# Large / personal data exports — keep local
*.pst
facebook-*/
facebook-*.zip
Card Photos/

# Office temp lock files
~$*
